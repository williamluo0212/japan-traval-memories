# 更新日志 / Changelog

本项目的所有重要变更都记录在此。

## v2.5 — 2026-08-14

### 修复
- iOS Safari：从子页面退回首页后无法再点进其他色页（bfcache 恢复时 `leaving` 锁
  与 `.opening` 类未复位，改用 `pageshow`（`persisted`）复位状态）

## v2.4 — 2026-08-14

### SEO / 社交分享
- 补齐 `<head>` 元信息：页面描述（description）、canonical、theme-color
- 新增 Open Graph + Twitter Card 标签：分享到微信/微博/X 时显示标题、描述与预览图
- 新增 favicon（五色竖条 SVG）、iOS 图标（apple-touch-icon）、分享预览图（og-image 1200×630）

## v2.3 — 2026-08-14

### 性能优化
- 照片输出格式从 JPEG 改为 **AVIF**：体积约为同质量 JPEG 的 60%，全站加载更快
  （尺寸/质量参数见 `scripts/lib/thumbs.js`；现代浏览器全面支持）
- 首页五色分屏背景由 CSS `background-image` 改为 `<img>` + 覆盖层：
  支持 `decoding="async"`、`object-fit` 铺满；色罩改用 `rgba()`（替代 `color-mix`，兼容性更好）
- 构建缓存升级 v2：manifest 增加版本门 + CI 缓存 key 加 `v2-avif`，旧 JPEG 缓存自动失效重建

### 维护
- 移除本地残留的 `dist/` 内嵌 git 仓库（旧 gh-pages 推送方式遗留，约 1.6GB），
  现由 GitHub Actions 从干净 checkout 构建发布，`dist/` 可随时整删重建

## v2.2 — 2026-07-23

### 移动端动效
- 首页五条色带入场依次浮现（错峰 0.08s）
- 点选颜色时，被点的色带在 0.6s 内舒展至约 400px 高、其余四条温和收窄
  （对应桌面端的悬停展开，屏风意象），随后进入颜色页
- 遵循系统「减弱动态效果」设置，开启时跳过全部动画直接跳转

## v2.1 — 2026-07-23

### 访客投稿功能（端到端验证上线）
- 新增「投稿」页：拖拽上传、双语界面、Turnstile 人机验证
- Cloudflare Worker 接收投稿：20MB 上限、按文件头识别真实格式（防伪造扩展名）、
  来源域名检查、可选每 IP 每日限频
- 投稿自动生成 Pull Request，站主合并=上线（自动归色+「投稿」标注）、关闭=拒绝
- 灯箱与照片说明中的「来访者投稿 / ご投稿」标注

## v2.0 — 2026-07-23

### 上线 GitHub Pages + 全自动部署
- 仓库重组为源码仓库（scripts / site / photos / workers），dist 改由 CI 生成
- GitHub Actions：推送 main 自动构建并发布 Pages，缩略图缓存加速增量构建
- HEIC 解码跨平台化：macOS 用系统 sips，CI（Linux）自动切换 heic-decode（wasm）
- 全站 73 张照片（约 800MB 原图）入库

## v1.2 — 2026-07-23

### 排版优化
- 照片页改为瀑布流：按时间序放入最短列，消除整行空白，间距收紧
  （灯箱切换仍严格按拍摄时间，不受排版重排影响）
- 首页五色分屏改用照片背景：构建时按「该色占比 ×（1−中性占比）」自动挑选
  各色最鲜明的一张，覆传统色薄罩保持五色识别

## v1.1 — 2026-07-23

### 首页改版
- 副标题定为「无尽夏 / 終わらぬ夏」
- 首页改为五色分屏色块（縦書き假名标注 + 照片计数），点击直达对应颜色页

## v1.0 — 2026-07-23

### 网站建成
- 五色分页：茜・藍・山吹・松葉・鼠（日本传统色命名），主色调自动归类
  （HSL 色相分桶、饱和度加权、低置信度提示），子目录强制指定
- 照片按 EXIF 拍摄时间升序；无 EXIF 回退文件时间并标 ※
- 灯箱：原生 dialog，方向键/滑动切换，URL 直达，EXIF 参数行
- 隐私：发布图片剥离全部 EXIF（含 GPS），仅页面展示拍摄参数
- 日式极简设计：生成り色底、明朝体、縦書き标题、中日双语并列
- 构建管线：sharp + exifr，增量缓存（内容 hash 输出），一步上传
  （拖入 photos/ 自动重建刷新），单张损坏不中断构建
